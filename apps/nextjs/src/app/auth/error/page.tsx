"use client";

const Error = () => {
  // const error = searchParams?.error;

  return (
    <div>
      <h1>Error</h1>
      {/* {error && <p>{error}</p>} */}
    </div>
  );
};

export default Error;
